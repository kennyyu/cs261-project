To compile waldo:

Make sure the BDB settings in defs.mk are correct. Then do "make".

------------------------------------------------------------------------
To run waldo:

1. Create a twig format file (with the kernel or pesto's waldify tool)

2. Run waldo
   Assuming the twig file is twig.wt
  ./waldo twig.wt

3. View the results
   wdb_dump db/
